{
  "description": "Dps avoid by request headers modification",
  "manifest_version": 2,
  "name": "requestModifier",
  "version": "1.0",

  "permissions": [
    "storage",
    "webRequest",
    "webRequestBlocking",
    "<all_urls>"
  ],

  "background": {
    "scripts": ["background.js"]
  }
}
